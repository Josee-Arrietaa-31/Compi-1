| --- Prueba de Gramática: Puntos A - I ---

| b. Variables globales
world int configuracion_base = 100 endl
world string nombre_chip = "Chip-X25" endl

| a. Creación de funciones (gift)
| q. Retorno de valores y parámetros con ¿ ?
gift int calcular_potencia ¿ int base, int exponente ?
¡
    | e. Variables locales y g. Uso de paréntesis ¿ ?
    local int resultado = ¿ base ^ exponente ? endl
    
    | i. Operadores unarios ++ y --
    resultado++ endl
    
    return resultado endl
!

| r. Método principal (navidad) tipo coal
gift coal navidad ¿ ?
¡
    | c. Tipos: int, float, bool, char, string
    local int voltios = 5 endl
    local float temperatura = 36.6 endl
    local bool encendido = true endl
    local char grupo = 'A' endl
    
    | d. Arreglos (creación con asignación ¡ !)
    local int matriz[2][2] = ¡ ¡ 1, 0 !, ¡ 0, 1 ! ! endl
    
    | --- Prueba de Divisiones (Punto H) ---
    local int resultado_entero = 10 // 3 endl    
    local float resultado_decimal = 10 / 3 endl  

    | j. Expresiones relacionales y lógicas (@, ~, Σ)
    decide of
        ¿ ¿ temperatura >= 40.0 ? @ Σ encendido ? ->
        ¡
            show "Alerta: Sobrecalentamiento" endl
        !
        else ->
        ¡
            | i. Negativo unario
            voltios = -1 endl
        !
    end decide endl
    
    return coal endl
!